process_kraken2 <- function(
    std_reports_path, mpa_reports_path,
    metadata_path, ref_db_path
) {

    std_reports <- load_STDreports(std_reports_path)
    mpa_reports <- load_MPAreports(mpa_reports_path)



}